# Pinned to the versions this domain was verified against. The Node side has
# package-lock.json; this is its counterpart.
-e ../core/python

pytest==9.1.1
pytest-bdd==8.1.0
pytest-playwright==0.9.0

# Pulled in by the three above; pinned so a resolver change cannot move them:
gherkin-official==29.0.0
greenlet==3.5.5
Mako==1.4.1
parse==1.22.1
playwright==1.62.0
pyee==13.0.1
pytest-base-url==2.1.0
